{
  "scripts": {
    "start": "nodemon server.mjs",
    "build": "rm -R static && client/npm run build && mv client/build static"
  },
  "dependencies": {
    "@apollo/server": "^4.10.0",
    "express": "^4.18.2",
    "graphql": "^16.8.1",
    "knex": "^3.1.0",
    "nodemon": "^3.0.2",
    "sqlite3": "^5.1.7"
  }
}
